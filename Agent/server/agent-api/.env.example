AGENT_DATA_DIR=/srv/agent-api/data
AGENT_SCRIPTS_DIR=/srv/agent-api/scripts
AGENT_CORS_ORIGINS=*

# Keep real keys on the server, not in Git.
QWEATHER_KEY=
QWEATHER_HOST=

# Home AI chat. Keep this key only on the backend server.
DEEPSEEK_API_KEY=
DEEPSEEK_API_BASE=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
